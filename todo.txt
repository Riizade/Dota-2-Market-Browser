Needs Testing
-------------------------------------------------------------------------------

- Updating database continuously/periodically
- Search panel request link

In Progress
-------------------------------------------------------------------------------

- Do parsing for slots, types, heroes, sets
- Use dictionaries for the replacement tables instead of linear searching lists
- Build a database table of item slots to prevent continuously downloading wiki pages
- Use Wiki to parse rarity, description

Not Started
-------------------------------------------------------------------------------

- Figure out what to do for inconsistent slots (beast being both mount (mirana) and ability slot (beastmaster), for instance)
- Display item description/details under big_image on hover
- Remove HTML character codes from items (&quot;, &lt;, etc)
- Create small resolution pictures for each item (96x63) to save on bandwidth
- Add a log parser (lists items with errors, etc)
- Make page numbers look prettier
- Periodically update schema as well as the market items